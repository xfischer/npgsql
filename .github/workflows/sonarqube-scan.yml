###
# Foundation-security SonarQube workflow
# version: dotnet-sonarqube
###
name: Foundation-Security/SonarQube .net Scan

on:
  push:
    tags:
      - "**"
    branches:
      - "*main*"
      - "*master*"
      - "*STABLE*"
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - "**"
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch to scan"
        required: true
        default: "master"

jobs:
  SonarQube-Scan:
    name: SonarQube Scan Job
    if: ${{ github.actor != 'dependabot[bot]' }}
    permissions:
      id-token: write
      contents: read
    runs-on: windows-latest
    steps:
      - name: Checkout source repository for dispatch runs
        id: checkout-source-dispatch
        if: github.event_name == 'workflow_dispatch'
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ inputs.ref }}
          path: source
          token: ${{ secrets.GH_SLONIK }}

      - name: Checkout source repository for non-dispatch runs
        id: checkout-source
        if: github.event_name != 'workflow_dispatch'
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: ${{ github.ref }}
          path: source
          token: ${{ secrets.GH_SLONIK }}

      - name: Set up JDK 17
        uses: actions/setup-java@v1
        with:
          java-version: 1.17

      - name: Cache SonarQube packages
        uses: actions/cache@v1
        with:
          path: ~\.sonar\cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Cache SonarQube scanner
        id: cache-sonar-scanner
        uses: actions/cache@v1
        with:
          path: .\.sonar\scanner
          key: ${{ runner.os }}-sonar-scanner
          restore-keys: ${{ runner.os }}-sonar-scanner

      - name: Install SonarQube scanner
        if: steps.cache-sonar-scanner.outputs.cache-hit != 'true'
        shell: powershell
        run: |
          New-Item -Path .\.sonar\scanner -ItemType Directory
          dotnet tool update dotnet-sonarscanner --tool-path .\.sonar\scanner

      - name: Checkout foundation repository
        uses: actions/checkout@v3
        with:
          repository: EnterpriseDB/foundation-packaging
          ref: v2.2.2
          token: ${{ secrets.GH_SLONIK }}
          path: foundation

      - name: Get EPAS Server
        uses: ./foundation/actions/windows/install/edbas
        with:
          edbas-version: 16
          additional-options: --superpassword edb

      - name: Create test database
        shell: cmd
        run: makedb.bat 5444
        working-directory: source/edb_test_db_scripts

      - name: Install PostGIS
        uses: ./foundation/actions/windows/install/postgis
        with:
          edbas-version: 16
          additional-options: --pgpassword edb --extract-only 1

      - name: Install DotNet Coverage tool
        shell: powershell
        run: |
          dotnet tool install --global dotnet-coverage

      - name: Build and analyze
        shell: powershell
        run: |
          .\.sonar\scanner\dotnet-sonarscanner begin /k:"EnterpriseDB_edb-dotnet" /n:"edb-dotnet" /v:"${{ github.ref_name }}" /d:sonar.token="${{ secrets.SONARQUBE_TOKEN }}" /d:sonar.host.url="${{ vars.SQ_URL }}" /d:sonar.verbose=true /d:sonar.cs.vscoveragexml.reportsPaths=coverage.xml
          dotnet build source/npgsql-8/ef.core/efcore.pg.sln --no-incremental -f net8.0
          dotnet-coverage collect "dotnet test source/npgsql-8/ef.core/efcore.pg.sln -c Release -f net8.0" -f xml -o "coverage.xml"
          .\.sonar\scanner\dotnet-sonarscanner end /d:sonar.token="${{ secrets.SONARQUBE_TOKEN }}"
